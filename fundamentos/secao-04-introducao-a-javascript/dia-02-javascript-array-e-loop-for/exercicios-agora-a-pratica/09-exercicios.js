let numbers = [];

for (index = 1 ; index < 26 ; index += 1) {
    numbers.push(index)
}

console.log(numbers)

for (index = 0 ; index < numbers.length ; index += 1) {
    console.log(numbers[index]/2)
}